SECRET_KEY=replace-with-a-random-secret-at-least-32-characters
DATABASE_URL=mysql+pymysql://username:password@hostname:4000/esurvey
DATABASE_SSL=true
# Set true once the database schema is initialized with: flask --app app init-db
DATABASE_READY=false
# LOCAL ONLY: SQLite is the default when VERCEL is not set.
